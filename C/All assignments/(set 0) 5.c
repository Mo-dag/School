#include <stdio.h>

int main()
{
    // Declare a variable for storing user input 
    int a;
    int b;
    // Inform the user about what to do
    printf("Input an integer value: ");
    scanf("%d", &a);
    printf("Input an other integer value :");
    scanf("%d", &b);
    // Get the input
    // How this works is explained later. For now, you have to learn this by heart.
	
    
    // Here we just print the input
	printf("Your input summa is : %d\n", a+b);
}
